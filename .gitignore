# 测试
tests/images

# ---- 编辑器 / 工具链 / 虚拟环境 ----
.claude/
.venv/
.vscode/
.idea/
.cargo-home/
.uv-cache/

# ---- 运行时输出 ----
trace/
docs/superpowers/
.ui-preview/
.tmp/

# ---- Python 编译产物 ----
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- 真实环境配置（含密钥 / 本地覆盖） ----
.env
model_services.json
AGENTS.md
config/.env
config/.env.local
config/config.json
config/model_services.json

# ---- Node / web 构建 ----
node_modules/
web/dist/

# ---- Rust / Tauri 构建产物 ----
# Cargo.lock 不入库（项目惯例，见 5edaeb9）；target 是 cargo build 产物；
# gen/ 是 `tauri build` 自动生成的 schema / capabilities 目录。
src-tauri/target/
src-tauri/gen/
src-tauri/Cargo.lock
src-tauri/binaries/
.dist/
# 临时占位图标（仅 766 字节的 .ico placeholder，等正式设计稿替换）
src-tauri/icons/icon.ico

# ---- 临时基准脚本 / 本地设计稿（不入库） ----
test.py
design-system/
